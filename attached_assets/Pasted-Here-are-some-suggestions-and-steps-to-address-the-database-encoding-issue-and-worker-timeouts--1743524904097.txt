Here are some suggestions and steps to address the database encoding issue and worker timeouts:

---

### 1. Verify Database and Connection Encoding

- **Database Collation and Charset:**  
  Ensure that your database and its tables are configured to use UTF-8 (or a suitable variant like utf8mb4 for MySQL). For example, if using MySQL, your table definition might look like:
  
  ```sql
  CREATE TABLE products (
      id INT AUTO_INCREMENT PRIMARY KEY,
      name VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci,
      ...
  );
  ```
  
- **Connection Parameters:**  
  Make sure your database connection string explicitly sets the charset. For instance, in SQLAlchemy for MySQL, use:
  
  ```python
  engine = create_engine("mysql+pymysql://user:password@host/dbname?charset=utf8mb4")
  ```
  
  This helps ensure that all transmitted data is treated as UTF-8.

---

### 2. Review and Optimize Greek Character Conversion

- **Ensure Unicode Handling:**  
  Verify that your conversion function is not inadvertently stripping all meaningful content. For Python 3, strings are Unicode by default, so focus on converting only as needed:
  
  ```python
  from unidecode import unidecode
  
  def convert_greek_to_ascii(text):
      # Ensure text is a proper Unicode string before conversion
      return unidecode(text)
  ```
  
- **Avoid Excessive Conversions:**  
  If the conversion is heavy, consider caching the results or pre-computing them during data ingestion rather than at query time. This can reduce the load during flush operations.

---

### 3. Manage Worker Timeouts and Batch Database Operations

- **Increase Worker Timeout:**  
  If the conversion and flush operations are taking too long, you might need to adjust your worker timeout settings (e.g., if using Gunicorn, increase the `timeout` parameter).

- **Batch Inserts/Flushes:**  
  Instead of flushing a large number of records at once, process the data in smaller batches. This can prevent timeouts and reduce memory overhead:
  
  ```python
  batch_size = 100
  for i in range(0, len(records), batch_size):
      batch = records[i:i + batch_size]
      session.bulk_save_objects(batch)
      session.commit()  # Commit each batch separately
  ```

- **Asynchronous Processing:**  
  Consider offloading heavy conversion tasks to a background worker (using tools like Celery). This way, your main application isn’t blocked during long-running operations.

---

### 4. Logging and Debugging

- **Log Problematic Strings:**  
  Temporarily add logging to capture the exact Greek strings that are causing issues. This will help identify if there’s any malformed data:
  
  ```python
  import logging
  
  logging.basicConfig(level=logging.DEBUG)
  
  def convert_greek_to_ascii(text):
      converted = unidecode(text)
      if not converted.strip():
          logging.debug(f"Conversion resulted in an empty string for: {text}")
      return converted
  ```

- **Test with Representative Data:**  
  Create unit tests using a variety of Greek inputs to see how your conversion function behaves. This can help catch cases where the conversion might produce empty strings.

---

By verifying your database settings, ensuring proper Unicode handling, optimizing batch processing, and possibly offloading heavy operations asynchronously, you can mitigate the encoding errors and avoid worker timeouts during the database flush operations. Let me know if you need further clarification or additional code examples for any of these steps!