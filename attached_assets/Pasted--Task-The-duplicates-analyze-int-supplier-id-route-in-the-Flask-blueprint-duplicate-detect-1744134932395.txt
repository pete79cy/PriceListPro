**Task:** The `/duplicates/analyze/<int:supplier_id>` route in the Flask blueprint `duplicate_detection.py` is currently returning a JSON response when the "Analyze with AI" button is clicked. The expected behavior is for this route to render the `analysis.html` template, displaying the AI's analysis of potential duplicate supplier products.

**Problem Description:** When a user navigates to the Supplier Product Duplicate Detection page, selects a supplier, sets a similarity threshold, and clicks "Analyze with AI", instead of being presented with a webpage containing the AI's analysis and options to act on the findings, the browser displays a JSON object. This JSON object contains the analysis results, the supplier ID, and the threshold.

**Goal:** Modify the `analyze_supplier_duplicates` function within the `duplicate_detection.py` Flask blueprint to correctly render the `analysis.html` template and pass the necessary data to it for display.

**Specific Instructions:**

1.  **Locate the `analyze_supplier_duplicates` function:** Find the function defined with the `@bp.route('/analyze/<int:supplier_id>', methods=['POST'])` decorator within the `duplicate_detection.py` file.

2.  **Identify the current return statement:** Examine what the function is currently returning. It is likely returning a `jsonify` object or a plain Python dictionary.

3.  **Replace the return statement:** Change the return statement to use the `render_template` function from Flask. The template to be rendered is `duplicate_detection/analysis.html`.

4.  **Pass necessary data to the template:** Ensure that the following data is passed as keyword arguments to the `render_template` function so that it can be used within the `analysis.html` template:
    * `supplier`: The `Supplier` object for the currently selected supplier. This is likely already being fetched using `Supplier.query.get_or_404(supplier_id)`.
    * `analysis_results`: The list of dictionaries containing the AI's analysis for each potential duplicate pair. This is being generated by the `ask_openai_for_resolution(duplicates)` call.
    * `threshold`: The similarity threshold used for finding the initial potential duplicates. This is being retrieved from the form using `request.form.get('threshold', ...)`.

5.  **Ensure the `analysis.html` template exists:** Verify that a file named `analysis.html` exists within a subdirectory named `duplicate_detection` inside your application's `templates` directory (e.g., `templates/duplicate_detection/analysis.html`). If it doesn't exist, you will need to create it. (You provided a basic example of this in the previous turn, which should be sufficient).

6.  **Review the `analysis.html` template:** Confirm that the `analysis.html` template is designed to receive and display the `supplier`, `analysis_results`, and `threshold` data correctly. It should iterate through the `analysis_results` and present the information to the user, potentially including a form to allow the user to act on the AI's suggestions (e.g., mark as duplicate).

7.  **Test the functionality:** After making the changes, run the Flask application and navigate to the Supplier Product Duplicate Detection page. Select a supplier, set a threshold, and click "Analyze with AI". Verify that you are now redirected to a webpage rendered by `analysis.html` displaying the AI's analysis results, instead of seeing raw JSON.

**Potential Issues to Consider:**

* **Template Path:** Double-check that the path to the template in `render_template` is correct (`'duplicate_detection/analysis.html'`).
* **Data Passed to Template:** Ensure the variable names used when passing data to `render_template` match the variable names used within the `analysis.html` template (e.g., `supplier=supplier`, not `current_supplier=supplier`).
* **Template Content:** If the `analysis.html` template is not yet created or is incomplete, the page might render with errors or not display the data correctly.

By following these detailed instructions, the Replit AI Agent should be able to identify the issue and modify the Flask route to correctly render the HTML template for displaying the AI analysis results.

